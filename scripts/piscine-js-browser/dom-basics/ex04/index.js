/**
 * Ex04 - Navigate DOM
 * Navigation dans l'arbre DOM
 */

/**
 * Récupère le parent d'un élément
 * @param {Element} element - Élément DOM
 * @returns {Element|null} Parent
 */
export function getParent(element) {
  void element;
  // TODO: parentElement
  return undefined;
}

/**
 * Récupère tous les enfants directs
 * @param {Element} element - Élément DOM
 * @returns {Element[]} Tableau d'enfants
 */
export function getChildren(element) {
  void element;
  // TODO: Convertir children en Array
  return undefined;
}

/**
 * Récupère le premier enfant élément
 * @param {Element} element - Élément DOM
 * @returns {Element|null} Premier enfant
 */
export function getFirstChild(element) {
  void element;
  // TODO: firstElementChild
  return undefined;
}

/**
 * Récupère le dernier enfant élément
 * @param {Element} element - Élément DOM
 * @returns {Element|null} Dernier enfant
 */
export function getLastChild(element) {
  void element;
  // TODO: lastElementChild
  return undefined;
}

/**
 * Récupère le frère précédent
 * @param {Element} element - Élément DOM
 * @returns {Element|null} Frère précédent
 */
export function getPrevSibling(element) {
  void element;
  // TODO: previousElementSibling
  return undefined;
}

/**
 * Récupère le frère suivant
 * @param {Element} element - Élément DOM
 * @returns {Element|null} Frère suivant
 */
export function getNextSibling(element) {
  void element;
  // TODO: nextElementSibling
  return undefined;
}

/**
 * Récupère tous les frères (siblings)
 * @param {Element} element - Élément DOM
 * @returns {Element[]} Frères (sans l'élément lui-même)
 */
export function getSiblings(element) {
  void element;
  // TODO: Filtrer les enfants du parent
  return undefined;
}

/**
 * Récupère tous les ancêtres jusqu'à la racine
 * @param {Element} element - Élément DOM
 * @returns {Element[]} Ancêtres (du plus proche au plus loin)
 */
export function getAncestors(element) {
  void element;
  // TODO: Remonter avec parentElement
  return undefined;
}

/**
 * Récupère l'index d'un élément parmi ses frères
 * @param {Element} element - Élément DOM
 * @returns {number} Index (0-based)
 */
export function getIndex(element) {
  void element;
  // TODO: Compter les previousElementSibling
  return undefined;
}

/**
 * Récupère le nième enfant
 * @param {Element} parent - Élément parent
 * @param {number} index - Index (0-based)
 * @returns {Element|null} Enfant à l'index
 */
export function getNthChild(parent, index) {
  void parent;
  void index;
  // TODO: children[index]
  return undefined;
}
