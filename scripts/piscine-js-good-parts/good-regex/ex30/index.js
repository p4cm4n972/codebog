/**
 * Ex30 - Character Classes
 * Classes de caractères et quantificateurs
 */

/**
 * Extrait tous les mots (lettres et chiffres)
 * @param {string} text - Texte
 * @returns {string[]} Mots
 */
export function extractWords(text) {
  void text;
  // TODO: Utiliser \w+
  return undefined;
}

/**
 * Extrait les caractères non-alphanumériques
 * @param {string} text - Texte
 * @returns {string[]} Caractères spéciaux
 */
export function extractSpecialChars(text) {
  void text;
  // TODO: Utiliser \W
  return undefined;
}

/**
 * Vérifie si une chaîne ne contient que des chiffres
 * @param {string} text - Texte
 * @returns {boolean} True si que des chiffres
 */
export function isOnlyDigits(text) {
  void text;
  // TODO: Utiliser ^\d+$
  return undefined;
}

/**
 * Vérifie si une chaîne est alphabétique (lettres seulement)
 * @param {string} text - Texte
 * @returns {boolean} True si que des lettres
 */
export function isAlphabetic(text) {
  void text;
  // TODO: Utiliser ^[a-zA-Z]+$
  return undefined;
}

/**
 * Extrait les voyelles
 * @param {string} text - Texte
 * @returns {string[]} Voyelles trouvées
 */
export function extractVowels(text) {
  void text;
  // TODO: Utiliser [aeiouAEIOU]
  return undefined;
}

/**
 * Masque tous les chiffres avec #
 * @param {string} text - Texte
 * @returns {string} Texte avec chiffres masqués
 */
export function maskDigits(text) {
  void text;
  // TODO: Remplacer \d par #
  return undefined;
}

/**
 * Normalise les espaces (multiples -> un seul)
 * @param {string} text - Texte
 * @returns {string} Texte normalisé
 */
export function normalizeSpaces(text) {
  void text;
  // TODO: Remplacer \s+ par un seul espace et trim
  return undefined;
}

/**
 * Valide un code hexadécimal (#RGB ou #RRGGBB)
 * @param {string} code - Code hex
 * @returns {boolean} True si valide
 */
export function isValidHexColor(code) {
  void code;
  // TODO: Pattern ^#([a-fA-F0-9]{3}|[a-fA-F0-9]{6})$
  return undefined;
}
