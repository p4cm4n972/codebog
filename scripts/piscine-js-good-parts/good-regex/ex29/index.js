/**
 * Ex29 - Regex Basics
 * test, match, replace
 */

/**
 * Vérifie si une chaîne contient un mot
 * @param {string} text - Texte à chercher
 * @param {string} word - Mot à trouver
 * @returns {boolean} True si trouvé
 */
export function containsWord(text, word) {
  void text;
  void word;
  // TODO: Utiliser new RegExp et test()
  return undefined;
}

/**
 * Compte les occurrences d'un pattern
 * @param {string} text - Texte
 * @param {string} pattern - Pattern (regex string)
 * @returns {number} Nombre d'occurrences
 */
export function countMatches(text, pattern) {
  void text;
  void pattern;
  // TODO: Utiliser match avec flag g
  return undefined;
}

/**
 * Extrait tous les nombres d'une chaîne
 * @param {string} text - Texte avec nombres
 * @returns {number[]} Nombres trouvés
 */
export function extractNumbers(text) {
  void text;
  // TODO: Utiliser /\d+/g et map
  return undefined;
}

/**
 * Remplace toutes les occurrences (case insensitive)
 * @param {string} text - Texte original
 * @param {string} search - Texte à chercher
 * @param {string} replacement - Remplacement
 * @returns {string} Texte modifié
 */
export function replaceAll(text, search, replacement) {
  void text;
  void search;
  void replacement;
  // TODO: Utiliser new RegExp avec flags gi
  return undefined;
}

/**
 * Met en majuscule la première lettre de chaque mot
 * @param {string} text - Texte
 * @returns {string} Texte en Title Case
 */
export function toTitleCase(text) {
  void text;
  // TODO: Utiliser replace avec callback
  return undefined;
}

/**
 * Censure un mot dans un texte
 * @param {string} text - Texte
 * @param {string} word - Mot à censurer
 * @returns {string} Texte censuré avec ***
 */
export function censor(text, word) {
  void text;
  void word;
  // TODO: Remplacer le mot par des *
  return undefined;
}

/**
 * Divise une chaîne par un ou plusieurs espaces
 * @param {string} text - Texte
 * @returns {string[]} Mots
 */
export function splitBySpaces(text) {
  void text;
  // TODO: Utiliser split avec /\s+/
  return undefined;
}
