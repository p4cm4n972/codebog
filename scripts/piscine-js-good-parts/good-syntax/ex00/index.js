/**
 * Ex00 - Strict Equality
 * Utiliser === et !== exclusivement
 */

/**
 * Vérifie si deux valeurs sont strictement égales
 * @param {any} a - Première valeur
 * @param {any} b - Deuxième valeur
 * @returns {boolean} true si strictement égales
 */
export function strictEquals(a, b) {
  void a;
  void b;
  // TODO: Retourner a === b
  return undefined;
}

/**
 * Vérifie si une valeur est strictement null
 * @param {any} value - Valeur à tester
 * @returns {boolean} true si value === null
 */
export function isNull(value) {
  void value;
  // TODO: Retourner value === null
  return undefined;
}

/**
 * Vérifie si une valeur est strictement undefined
 * @param {any} value - Valeur à tester
 * @returns {boolean} true si value === undefined
 */
export function isUndefined(value) {
  void value;
  // TODO: Retourner value === undefined
  return undefined;
}

/**
 * Vérifie si une valeur est null ou undefined (nullish)
 * @param {any} value - Valeur à tester
 * @returns {boolean} true si null ou undefined
 */
export function isNullish(value) {
  void value;
  // TODO: Retourner true si value === null || value === undefined
  return undefined;
}

/**
 * Compare le type et la valeur
 * @param {any} a - Première valeur
 * @param {any} b - Deuxième valeur
 * @returns {Object} {sameType, sameValue, strictlyEqual}
 */
export function compare(a, b) {
  void a;
  void b;
  // TODO: Retourner les 3 comparaisons
  return undefined;
}

/**
 * Trouve l'index d'un élément avec égalité stricte
 * @param {Array} arr - Tableau
 * @param {any} value - Valeur à trouver
 * @returns {number} Index ou -1
 */
export function strictIndexOf(arr, value) {
  void arr;
  void value;
  // TODO: Implémenter indexOf avec ===
  return undefined;
}
