/**
 * Ex24 - Array Methods
 * map, filter, reduce
 */

/**
 * Double chaque nombre
 * @param {number[]} numbers - Tableau de nombres
 * @returns {number[]} Nombres doublés
 */
export function doubleAll(numbers) {
  void numbers;
  // TODO: Utiliser map
  return undefined;
}

/**
 * Extrait une propriété de chaque objet
 * @param {Object[]} objects - Tableau d'objets
 * @param {string} key - Clé à extraire
 * @returns {any[]} Valeurs extraites
 */
export function pluck(objects, key) {
  void objects;
  void key;
  // TODO: Utiliser map
  return undefined;
}

/**
 * Garde seulement les nombres pairs
 * @param {number[]} numbers - Tableau de nombres
 * @returns {number[]} Nombres pairs
 */
export function evensOnly(numbers) {
  void numbers;
  // TODO: Utiliser filter
  return undefined;
}

/**
 * Retire les valeurs falsy
 * @param {any[]} arr - Tableau mixte
 * @returns {any[]} Tableau sans valeurs falsy
 */
export function compact(arr) {
  void arr;
  // TODO: Utiliser filter(Boolean)
  return undefined;
}

/**
 * Calcule la somme
 * @param {number[]} numbers - Tableau de nombres
 * @returns {number} Somme
 */
export function sum(numbers) {
  void numbers;
  // TODO: Utiliser reduce
  return undefined;
}

/**
 * Calcule la moyenne
 * @param {number[]} numbers - Tableau de nombres
 * @returns {number} Moyenne (0 si vide)
 */
export function average(numbers) {
  void numbers;
  // TODO: Utiliser reduce
  return undefined;
}

/**
 * Groupe par une propriété
 * @param {Object[]} items - Tableau d'objets
 * @param {string} key - Clé de groupement
 * @returns {Object} Objets groupés
 */
export function groupBy(items, key) {
  void items;
  void key;
  // TODO: Utiliser reduce pour grouper
  return undefined;
}

/**
 * Compte les occurrences
 * @param {any[]} items - Tableau d'éléments
 * @returns {Object} { item: count }
 */
export function countOccurrences(items) {
  void items;
  // TODO: Utiliser reduce pour compter
  return undefined;
}
