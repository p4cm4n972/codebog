/**
 * Ex26 - Array Transform
 * flat, flatMap, sort, reverse
 */

/**
 * Aplatit un tableau d'un niveau
 * @param {any[][]} arr - Tableau imbriqué
 * @returns {any[]} Tableau aplati
 */
export function flatten(arr) {
  void arr;
  // TODO: Utiliser flat()
  return undefined;
}

/**
 * Aplatit complètement un tableau
 * @param {any[]} arr - Tableau profondément imbriqué
 * @returns {any[]} Tableau complètement aplati
 */
export function flattenDeep(arr) {
  void arr;
  // TODO: Utiliser flat(Infinity)
  return undefined;
}

/**
 * Extrait et aplatit les tags de plusieurs articles
 * @param {Object[]} articles - { title, tags: string[] }
 * @returns {string[]} Tous les tags
 */
export function getAllTags(articles) {
  void articles;
  // TODO: Utiliser flatMap
  return undefined;
}

/**
 * Trie des nombres en ordre croissant (immutable)
 * @param {number[]} numbers - Tableau de nombres
 * @returns {number[]} Tableau trié (nouveau)
 */
export function sortNumbers(numbers) {
  void numbers;
  // TODO: Créer une copie et trier
  return undefined;
}

/**
 * Trie des objets par une propriété
 * @param {Object[]} items - Tableau d'objets
 * @param {string} key - Propriété de tri
 * @param {string} order - 'asc' ou 'desc'
 * @returns {Object[]} Tableau trié (nouveau)
 */
export function sortBy(items, key, order = 'asc') {
  void items;
  void key;
  void order;
  // TODO: Trier par propriété
  return undefined;
}

/**
 * Inverse un tableau (immutable)
 * @param {any[]} arr - Tableau
 * @returns {any[]} Tableau inversé (nouveau)
 */
export function reverseArray(arr) {
  void arr;
  // TODO: Créer une copie et inverser
  return undefined;
}

/**
 * Déduplique un tableau
 * @param {any[]} arr - Tableau avec doublons
 * @returns {any[]} Tableau sans doublons
 */
export function unique(arr) {
  void arr;
  // TODO: Utiliser Set
  return undefined;
}

/**
 * Intersection de deux tableaux
 * @param {any[]} a - Premier tableau
 * @param {any[]} b - Deuxième tableau
 * @returns {any[]} Éléments communs
 */
export function intersection(a, b) {
  void a;
  void b;
  // TODO: Éléments présents dans les deux
  return undefined;
}
