/**
 * Ex25 - Array Search
 * find, findIndex, includes, some, every
 */

/**
 * Trouve un utilisateur par ID
 * @param {Object[]} users - Tableau d'utilisateurs { id, name }
 * @param {number} id - ID à chercher
 * @returns {Object|undefined} Utilisateur trouvé
 */
export function findUserById(users, id) {
  void users;
  void id;
  // TODO: Utiliser find
  return undefined;
}

/**
 * Trouve l'index d'un élément
 * @param {any[]} arr - Tableau
 * @param {Function} predicate - Fonction de test
 * @returns {number} Index ou -1
 */
export function findIndexWhere(arr, predicate) {
  void arr;
  void predicate;
  // TODO: Utiliser findIndex
  return undefined;
}

/**
 * Vérifie si un tableau contient une valeur
 * @param {any[]} arr - Tableau
 * @param {any} value - Valeur à chercher
 * @returns {boolean} True si présent
 */
export function contains(arr, value) {
  void arr;
  void value;
  // TODO: Utiliser includes
  return undefined;
}

/**
 * Vérifie si au moins un élément satisfait le prédicat
 * @param {any[]} arr - Tableau
 * @param {Function} predicate - Fonction de test
 * @returns {boolean} True si au moins un
 */
export function hasAny(arr, predicate) {
  void arr;
  void predicate;
  // TODO: Utiliser some
  return undefined;
}

/**
 * Vérifie si tous les éléments satisfont le prédicat
 * @param {any[]} arr - Tableau
 * @param {Function} predicate - Fonction de test
 * @returns {boolean} True si tous
 */
export function hasAll(arr, predicate) {
  void arr;
  void predicate;
  // TODO: Utiliser every
  return undefined;
}

/**
 * Trouve le premier élément supérieur à une valeur
 * @param {number[]} numbers - Tableau de nombres
 * @param {number} threshold - Seuil
 * @returns {number|undefined} Premier nombre > threshold
 */
export function findFirstAbove(numbers, threshold) {
  void numbers;
  void threshold;
  // TODO: Utiliser find
  return undefined;
}

/**
 * Vérifie si un tableau contient tous les éléments d'un autre
 * @param {any[]} arr - Tableau source
 * @param {any[]} values - Valeurs à vérifier
 * @returns {boolean} True si tous présents
 */
export function containsAll(arr, values) {
  void arr;
  void values;
  // TODO: Utiliser every et includes
  return undefined;
}

/**
 * Vérifie si un tableau contient au moins un élément d'un autre
 * @param {any[]} arr - Tableau source
 * @param {any[]} values - Valeurs à vérifier
 * @returns {boolean} True si au moins un présent
 */
export function containsAny(arr, values) {
  void arr;
  void values;
  // TODO: Utiliser some et includes
  return undefined;
}
