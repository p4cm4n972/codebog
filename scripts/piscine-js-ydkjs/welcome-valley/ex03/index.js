/**
 * Ex03 - Arrays Introduction
 * Les bases des tableaux en JavaScript
 */

/**
 * Crée un array et retourne sa longueur
 * @returns {number}
 */
export function createArray() {
  // TODO: Crée fruits = ["apple", "banana", "cherry"]
  // Retourne fruits.length
  return undefined;
}

/**
 * Accès aux éléments par index
 * @returns {[string, string, undefined]}
 */
export function accessElements() {
  // TODO: Crée colors = ["red", "green", "blue"]
  // Retourne [colors[0], colors[2], colors[10]]
  return undefined;
}

/**
 * Modifier un array avec push et unshift
 * @returns {number[]}
 */
export function modifyArray() {
  // TODO: Crée numbers = [1, 2, 3]
  // Ajoute 4 à la fin avec push()
  // Ajoute 0 au début avec unshift()
  // Retourne le tableau modifié
  return undefined;
}

/**
 * Méthodes map, filter, reduce
 * @returns {[number[], number[], number]}
 */
export function arrayMethods() {
  // TODO: Crée arr = [1, 2, 3, 4, 5]
  // doubled = arr.map(x => x * 2)
  // evens = arr.filter(x => x % 2 === 0)
  // sum = arr.reduce((acc, x) => acc + x, 0)
  // Retourne [doubled, evens, sum]
  return undefined;
}

/**
 * Spread operator pour combiner des arrays
 * @returns {number[]}
 */
export function spreadArray() {
  // TODO: Crée arr1 = [1, 2] et arr2 = [3, 4]
  // Retourne [...arr1, ...arr2]
  return undefined;
}
